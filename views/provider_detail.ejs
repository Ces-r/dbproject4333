<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= provider.username %> - Provider Profile</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css">
    <style>
        .star-rating { color: #ffc107; font-size: 1.2rem; }
        .review-card { margin-bottom: 1rem; }
        .rating-form { background: #f8f9fa; padding: 1.5rem; border-radius: 8px; }
    </style>
</head>
<body>
    <div class="container mt-4">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="/dashboard">Dashboard</a></li>
                <li class="breadcrumb-item"><a href="/providers">All Providers</a></li>
                <li class="breadcrumb-item active"><%= provider.username %></li>
            </ol>
        </nav>

        <div class="row">
            <div class="col-md-8">
                <h1>
                    <%= provider.username %>
                    <% if (provider.is_verified) { %>
                        <span class="badge badge-success" title="Background Verified">
                            ✓ Verified
                        </span>
                    <% } %>
                </h1>
                
                <!-- Rating Summary -->
                <div class="mb-3">
                    <% if (avgRating) { %>
                        <span class="star-rating">
                            <% for (let i = 1; i <= 5; i++) { %>
                                <% if (i <= Math.floor(avgRating)) { %>
                                    ★
                                <% } else if (i - avgRating < 1) { %>
                                    ☆
                                <% } else { %>
                                    ☆
                                <% } %>
                            <% } %>
                        </span>
                        <strong><%= avgRating %></strong> 
                        (<%= totalReviews %> <%= totalReviews === 1 ? 'review' : 'reviews' %>)
                    <% } else { %>
                        <span class="text-muted">No reviews yet</span>
                    <% } %>
                </div>

                <!-- Provider Description -->
                <div class="card mb-4">
                    <div class="card-body">
                        <h5 class="card-title">About</h5>
                        <% if (provider.description) { %>
                            <p><%= provider.description %></p>
                        <% } else { %>
                            <p class="text-muted">No description available.</p>
                        <% } %>
                        
                        <!-- Book Service Button (Customers Only) -->
                        <% if (currentUser.role === 'C') { %>
                            <hr>
                            <a href="/providers/<%= provider.id %>/book" class="btn btn-primary btn-lg btn-block">
                                📅 Book Service with <%= provider.username %>
                            </a>
                        <% } %>
                    </div>
                </div>

                <!-- Leave a Review -->
                <% if (currentUser.role === 'C' && !hasReviewed) { %>
                    <div class="rating-form mb-4">
                        <h5>Leave a Review</h5>
                        <form action="/providers/<%= provider.id %>/review" method="POST">
                            <div class="form-group">
                                <label for="rating">Rating:</label>
                                <select name="rating" id="rating" class="form-control" required>
                                    <option value="">Select rating...</option>
                                    <option value="5">★★★★★ Excellent</option>
                                    <option value="4">★★★★☆ Good</option>
                                    <option value="3">★★★☆☆ Average</option>
                                    <option value="2">★★☆☆☆ Poor</option>
                                    <option value="1">★☆☆☆☆ Terrible</option>
                                </select>
                            </div>
                            <div class="form-group">
                                <label for="comment">Comment:</label>
                                <textarea name="comment" id="comment" class="form-control" rows="4" placeholder="Share your experience (optional)"></textarea>
                            </div>
                            <button type="submit" class="btn btn-primary">Submit Review</button>
                        </form>
                    </div>
                <% } else if (hasReviewed) { %>
                    <div class="alert alert-info">
                        You have already reviewed this provider. You can update your review below.
                    </div>
                    <div class="rating-form mb-4">
                        <h5>Update Your Review</h5>
                        <form action="/providers/<%= provider.id %>/review" method="POST">
                            <div class="form-group">
                                <label for="rating">Rating:</label>
                                <select name="rating" id="rating" class="form-control" required>
                                    <option value="">Select rating...</option>
                                    <option value="5">★★★★★ Excellent</option>
                                    <option value="4">★★★★☆ Good</option>
                                    <option value="3">★★★☆☆ Average</option>
                                    <option value="2">★★☆☆☆ Poor</option>
                                    <option value="1">★☆☆☆☆ Terrible</option>
                                </select>
                            </div>
                            <div class="form-group">
                                <label for="comment">Comment:</label>
                                <textarea name="comment" id="comment" class="form-control" rows="4" placeholder="Share your experience (optional)"></textarea>
                            </div>
                            <button type="submit" class="btn btn-primary">Update Review</button>
                        </form>
                    </div>
                <% } else if (currentUser.role === 'P') { %>
                    <div class="alert alert-secondary">
                        Only customers can leave reviews for providers.
                    </div>
                <% } %>

                <!-- Reviews List -->
                <h4 class="mb-3">Reviews</h4>
                <% if (reviews.length === 0) { %>
                    <div class="alert alert-light">
                        No reviews yet. Be the first to review!
                    </div>
                <% } else { %>
                    <% reviews.forEach(function(review) { %>
                        <div class="card review-card">
                            <div class="card-body">
                                <div class="d-flex justify-content-between">
                                    <h6 class="card-subtitle mb-2">
                                        <%= review.reviewer_name %>
                                    </h6>
                                    <small class="text-muted">
                                        <%= new Date(review.created_at).toLocaleDateString() %>
                                    </small>
                                </div>
                                <div class="star-rating mb-2">
                                    <% for (let i = 1; i <= 5; i++) { %>
                                        <%= i <= review.rating ? '★' : '☆' %>
                                    <% } %>
                                </div>
                                <% if (review.comment) { %>
                                    <p class="card-text"><%= review.comment %></p>
                                <% } %>
                            </div>
                        </div>
                    <% }); %>
                <% } %>
            </div>

            <!-- Sidebar -->
            <div class="col-md-4">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Contact</h5>
                        <p class="text-muted">Contact details will be shared after booking.</p>
                        <% if (currentUser.role === 'C') { %>
                            <a href="#" class="btn btn-success btn-block">Request Service</a>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>